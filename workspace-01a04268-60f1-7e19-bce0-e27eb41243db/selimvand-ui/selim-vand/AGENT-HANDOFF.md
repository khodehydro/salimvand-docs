# سند تحویل به ایجنت پیاده‌سازی — فروشگاه سلیم‌وند

> نسخهٔ ۱ · این سند «لایهٔ بصری و تجربهٔ کاربری» پروژه را برای پیاده‌سازی صفر‌تا‌صد در ریپازیتوری گیت‌هاب مشخص می‌کند.
> مکملِ سند مادر معماری است، نه جایگزین آن.

## 🔗 منابع (به همین ترتیب بخوان)

| # | منبع | لینک خام | نقش |
|---|---|---|---|
| ۱ | سند مادر معماری (آینه) | https://paste.opensuse.org/pastes/8da8303e19c3/raw | قوانین بک‌اند، دیتابیس، API، فازبندی (معتبرترین منبع منطق). لینک اصلی کارفرما: https://dpaste.com/4GNP536QM.txt |
| ۲ | **مرجع بصری (UI Concept)** | https://paste.opensuse.org/pastes/c6a9c450aab0/raw | یک فایل HTML خودکفا: طراحی کامل سایت عمومی + پنل مدیریت + سیستم طراحی، با هر دو پوستهٔ روشن/تاریک. آن را با نام `docs/ui-reference.html` ذخیره و در مرورگر باز کن |
| ۳ | همین سند | (لینکی که این متن را از آن خواندی) | توکن‌های دقیق، قواعد، نگاشت فازها، چک‌لیست پذیرش |

**اولویت در تعارض:** منطق/امنیت/داده ← سند مادر؛ ظاهر/رنگ/چیدمان/متن‌های رابط ← مرجع بصری + همین سند.

## 🎨 توکن‌های طراحی (دقیق — منبع حقیقت رنگ‌ها)

پالت برند (هر دو پوسته مشترک):
```
brand-950 #04121f | 900 #071c30 | 850 #0a2440 | 800 #0d2b4b | 700 #124270
brand-600 #175894 | 500 #1f6fbe | 400 #3f8ede | 300 #7fb5ea | 200 #bcd7f5
brand-100 #e0ecfa | 50 #f1f6fd
```
پوستهٔ روشن:
```
bg #f3f6fa | surface #ffffff | surface-2 #f7fafd | surface-3 #eef3f9
border #dce4ee | border-2 #c8d4e3 | text #0b1c2f | text-2 #4a5f79 | text-3 #8095ad
primary #0d2b4b (hover #0a2440) | link #175894
ok #0f8f52 (soft #e3f5ea) | warn #b9770b (soft #fdf0d9) | danger #c8383c (soft #fbe7e8)
```
پوستهٔ تاریک:
```
bg #050b14 | surface #0d1826 | surface-2 #111f31 | surface-3 #16283c
border #1d3049 | border-2 #294061 | text #eaf1f9 | text-2 #9db1c8 | text-3 #6c8299
primary #2f7fce (hover #4d99e0, fg #04101c) | link #69a8e6
ok #3ecf8e (soft #0d2f24) | warn #f0b454 (soft #332509) | danger #ff7a7f (soft #3a1618)
```
هندسه/سایه: شعاع دکمه ۱۰px، کارت ۱۴px، بج pill؛ سایه‌ها ملایم (۲ سطح + سایهٔ مودال)؛
اسپیسینگ ۴/۸/۱۲/۱۶/۲۴/۳۲/۴۸. سایدبار پنل در هر دو پوسته «سرمه‌ای تیره» (brand-900) است.

**تایپوگرافی:** فونت وزیرمتن (پکیج npm `vazirmatn`؛ وب‌فونت UI-FD = اعداد فارسی) وزن‌های ۴۰۰/۵۰۰/۶۰۰/۷۰۰.
اعداد رابط همیشه فارسی؛ کدها/بارکد/شمارهٔ فنی لاتین و مونو (`ui-monospace`) و در قالب LTR ایزوله.
RTL کامل (`dir=rtl`)؛ کشوها/شیت‌ها از لبهٔ inline-end؛ فونت فال‌بک Tahoma.
**فونت مرجع بصری base64 شده؛ در پیاده‌سازی از پکیج npm استفاده کن، نه base64.**

## 🧩 قواعد وضعیت و اجزا

- وضعیت موجودی: موجود=ok سبز · موجودِ کم/به‌زودی=warn کهربایی · ناموجود=danger قرمز · توقف=خنثی.
- چیپ برند در کارت: موجود = سبزِ ملایم + تیک؛ ناموجود = خاکستری + خط‌خورده.
- هیچ قیمت، قفسه یا دادهٔ داخلی در سایت عمومی نمایش داده نمی‌شود؛ پیام ثابت «قیمت فقط با استعلام» در هیرو و فوتر.
- اجزای پایه (با همین توکن‌ها پیاده شوند): btn (primary/outline/soft/ghost/danger + sm/lg/icon)، badge، kpi-card،
  table، field/inp/sel/search، chk/sw/seg، tabs، card، sheet (کشوی کناری)، modal/palette، tl (تایم‌لاین دفتر تراکنش‌ها)،
  stockbar، donut/bar chart (recharts با رنگ‌های brand)، toast، skeleton، empty-state.
- دارک‌مود فقط با توکن (`data-theme`) — کپی‌پیست رنگ ممنوع؛ هر صفحهٔ جدید از روز اول هر دو پوسته را دارد.

## 🗺 نقشهٔ صفحه‌ها (متناظر با بخش‌های `ui-reference.html`)

**سایت عمومی** — فقط دو مسیر:
- `/` : هدر (لوگو، منوی لنگر، CTA تماس) · هیرو سرمه‌ای (تیتر، چیپ خودروها، دکمه‌های تماس/تلگرام/بله، یادداشت قیمت، کارت اعتماد) ·
  ویدئوی آپارات (iframe فقط پس از کلیک) · کاتالوگ: فیلتر چسبان (برند/مدل/تیپ + دستهٔ درختی + جست‌وجو + فقط‌موجود + پاک‌کردن،
  وضعیت در URL) + گرید کارت (تصویر/بج دسته/بج وضعیت/برندها/خودروهای سازگار/کد لاتین/اکشن‌ها) + صفحه‌بندی بی‌نهایت ·
  تماس و آدرس (لیست اطلاعات + نقشه + پیام نوسان) · فوتر سرمه‌ای. موبایل: فیلتر جمع‌شونده + نوار تماس چسبان پایین.
- `/invoice/[token]` : هدر سرمه‌ای + PDF/چاپ · کارت متا (شماره، تاریخ شمسی، مشتری، خودرو، اعتبار لینک، QR) ·
  جدول اقلام با ستون برند · جعبه‌های پرداخت (نقدی/کارت) · پنل جمع سرمه‌ای (پرداخت‌شده/باقی‌مانده) · شرایط + اکشن‌ها.

**پنل مدیریت** (`admin.`): ورود (پس‌زمینهٔ سرمه‌ای) · چیدمان: سایدبار سرمه‌ای راست با گروه‌ها و شمارنده‌ها + منوی نقش‌محور،
تاپ‌بار با جست‌وجوی سراسری Ctrl+K (پالت گروه‌بندی‌شده ۱..۵)، اعلان‌ها، چیپ کاربر ·
داشبورد (۴ KPI شامل کارت هشدار کمبود، نمودار میله‌ای ۳۰ روز، دونات ترکیب موجودی، جدول بدهکاران + ارسال پیامک،
لیست کمبود با قفسه، تایم‌لاین تراکنش‌ها، کارت سلامت یکپارچه‌سازی‌ها) ·
صدور فاکتور (نوار مشتری با بدهی، جست‌وجوی زنده = ورودی بارکد Enter + اسکن دوربین، نتایج برند×موجودی×قفسه،
جدول ردیف‌ها با استپر/تخفیف، پنل مبالغ: پرداخت چندروش، بدهی خودکار، دکمهٔ صدور+پیامک، مودال موفقیت با لینک فاکتور و «فاکتور بعدی») ·
محصول (۵ تب: پایه/تصاویر WebP دو اندازه/شناسهٔ آپارات/درخت خودروها/اقلام برند با بارکد EAN و آستانه؛ حذف نرم) ·
انبار (KPI، پیلهای فیلتر سریع، جدول با stockbar و مسیر قفسه، شیت اصلاح موجودی با دلیل اجباری + تاریخچه) ·
فاکتورها (پیلهای وضعیت، ستون بدهی، اکشن pdf/پیامک/نمایش) · گزارش‌ها (KPI سود، نمودار ماهانه، دونات برند، جدول سود، خروجی) ·
تنظیمات (اطلاعات فروشگاه=خوراک سایت، قالب پیامک متغیردار، تلگرام/بله/کانال، آستانه‌ها، بکاپ) ·
موبایل پنل: نوار پایین + FAB صدور فاکتور.

##  نگاشت فازها (روی نقشهٔ راه سند مادر)

- **فاز ۰:** علاوه بر زیرساخت، `packages/ui` را بساز: فایل توکن‌های بالا + ThemeProvider (لایت/دارک) + اجزای پایه.
  همچنین `docs/ui-reference.html` (مرجع بصری) و این سند را داخل ریپازیتوری کامیت کن و در صورت امکان
  GitHub Pages را فعال کن تا نسخهٔ رندرشدهٔ مرجع بصری دائمی در دسترس بماند.
  پذیرش: صفحهٔ نمونهٔ توکن‌ها در هر دو پوسته رندر شود.
- **فاز ۱:** سایت `/` کامل + پنلِ محصولات/انبار طبق نقشه. پذیرش بصری: تطابق با شات‌های کاتالوگ/انبار/محصول مرجع.
- **فاز ۲:** صدور فاکتور + فاکتور آنلاین + PDF سرمه‌ای/شمسی/QR. پذیرش: سناریوی سند مادر + تطابق بصری مودال و صفحهٔ فاکتور.
- **فاز ۳:** داشبورد کامل، گزارش‌ها، پالت Ctrl+K. · **فاز ۴:** صفحهٔ تنظیمات و سلامت یکپارچه‌سازی‌ها.
- **فاز ۵:** دارک‌مود از قبل طراحی‌شده است؛ فقط تضمین کن صفحهٔ جدید توکن‌محور است + میانبرهای کیبورد.

## ✅ چک‌لیست پذیرش بصری هر PR

1. هر صفحهٔ جدید/تغییریافته در **هر دو پوسته** و در **دسکتاپ+موبایل** بازبینی شود.
2. هیچ رنگ خام (hex دلخواه) در کامپوننت‌ها نباشد؛ فقط توکن. ۳. اعداد فارسی؛ کدها لاتین‌مونو.
4. وضعیت‌ها رنگ معنایی درست دارند؛ سایت هیچ قیمتی نشان نمی‌دهد. ۵. RTL سالم (آیکن‌ها/کشوها/جدول‌ها).

## 📋 پرامپت پیشنهادی برای شروع ایجنت (کپی کن)

```
تو ایجنت پیاده‌سازی پروژهٔ «فروشگاه سلیم‌وند» در این ریپازیتوری هستی.
۱) سند مادر معماری را بخوان: https://paste.opensuse.org/pastes/8da8303e19c3/raw
۲) سند تحویل بصری را بخوان: <لینک همین سند را کارفرما می‌دهد>
۳) فایل مرجع بصری را بگیر و با نام docs/ui-reference.html کامیت کن:
   https://paste.opensuse.org/pastes/c6a9c450aab0/raw
و آن را در مرورگر باز کن؛ طراحی سایت+پنل+سیستم‌طراحی با لایت/دارک آنجاست.
۴) همین دو فایل (مرجع بصری + سند تحویل) را در فاز ۰ داخل docs/ ریپازیتوری نگه دار
و در صورت امکان GitHub Pages را فعال کن تا نسخهٔ رندرشده دائمی شود.
فازهای ۰ تا ۵ نقشهٔ راه را به‌ترتیب اجرا کن؛ قبل از ساخت هر صفحه، بخش متناظر مرجع بصری را ببین
و فقط از توکن‌های سند تحویل استفاده کن. هر فاز = PR + تست‌های معیار پذیرش. بدون تست، مرج ممنوع.
```
